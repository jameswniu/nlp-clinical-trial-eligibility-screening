version: "3.9"

services:
  screening:
    build: .
    container_name: clinical-trial-screening
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    command: python src/orchestrator.py
